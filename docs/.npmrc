# The theme is a git dependency. npm 11 gates those behind an install policy
# whose default is tightening: Renovate's sandbox already runs with it off
# (EALLOWGIT), which is why its lockfile updates for this dep fail. `root`
# permits the git deps this package.json declares and no transitive ones.
allow-git=root
