required_keys: [headscale_subdomain]
subdomain: hs
version:
  value: "0.29.4"
  datasource: github-releases
  depName: "juanfont/headscale"
  extractVersion: "^v(?<version>.+)$"
backup:
  systemd_services: [headscale]
  paths: [/var/lib/headscale]
  owner: [headscale, headscale]
units: [headscale]
