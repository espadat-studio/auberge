required_keys:
  - colporteur_feeds_password
  - colporteur_subdomain
subdomain: colporteur
version:
  value: "0.6.6"
  datasource: github-releases
  depName: "espadat-studio/colporteur"
  extractVersion: "^v(?<version>.+)$"
units: [colporteur, colporteur.timer]
