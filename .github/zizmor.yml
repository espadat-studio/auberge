rules:
  unpinned-uses:
    config:
      policies:
        "*": hash-pin
  artipacked:
    # renovate-checksums pushes the digest commit over the origin credential
    ignore:
      - renovate-checksums.yml
